<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Howdy, <%= @user.name %>!</h1>
    <p>There has been a new reply in <a href="<%= conversation_url(@reply.conversation.id) %>"><%= @reply.conversation.title %></a> by <%= @reply.author.first_name + " " + @reply.author.last_name %>:</p>
    <p><%= parse_text @reply.body %></p>
    
    <p>-----------------</p>
    <p>Unsubscribe from email notifications about new replies in this conversation: <a href="<%= unsubscribe_url(@reply.conversation.id) %>">Unsubscribe</a> </p>
  </body>
</html>